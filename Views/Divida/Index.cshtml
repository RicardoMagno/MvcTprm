@model IEnumerable<MvcTprm.Models.Divida>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClienteId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClienteNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorDaDivida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClienteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClienteNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorDaDivida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DividaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DividaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DividaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
